# Rust Backend Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the Rust backend of estudoHtmx (lista de despesas com htmx) per the shared design spec, sharing `shared/expenses.db` with the other 4 stacks.

**Architecture:** Axum 0.7 router servindo paginas HTML renderizadas com templates Askama compile-time-checked, banco SQLite via rusqlite (bundled). Estado da aplicacao compartilha `Arc<Mutex<Connection>>` (single-user study app, sem concorrencia real). Integracao HTMX manual: handlers leem `hx-request` e escrevem headers via `axum::http::HeaderMap`. POST `/expenses` retorna fragmento composto (form + tbody com `hx-swap-oob`).

**Tech Stack:** Rust 2021, Axum 0.7, Askama 0.12 + askama_axum 0.4, Tokio 1 (full), rusqlite 0.32 (bundled), evalexpr 11, serde 1, serde_urlencoded 0.7, tower 0.5. Porta 5005.

---

### Task 1: Project setup

**Files:**
- Create: `rust/Cargo.toml`
- Create: `rust/.gitignore`
- Create: `rust/src/main.rs` (placeholder)

- [ ] **Step 1: cargo init**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
cd rust
cargo init --name estudohtmx-rust
cd ..
```

- [ ] **Step 2: editar `rust/Cargo.toml` com as deps**

Substitua o conteudo de `rust/Cargo.toml` por:
```toml
[package]
name = "estudohtmx-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["full"] }
askama = "0.12"
askama_axum = "0.4"
rusqlite = { version = "0.32", features = ["bundled"] }
evalexpr = "11"
serde = { version = "1", features = ["derive"] }
serde_urlencoded = "0.7"
tower = "0.5"

[profile.dev]
opt-level = 0
```

- [ ] **Step 3: `rust/.gitignore`**

Crie `rust/.gitignore`:
```
/target
**/*.rs.bk
Cargo.lock
```

- [ ] **Step 4: `rust/src/main.rs` placeholder pra compilar**

Substitua `rust/src/main.rs` por:
```rust
fn main() {
    println!("estudohtmx-rust placeholder");
}
```

- [ ] **Step 5: build pra baixar deps**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
cd rust
cargo build
cd ..
```

- [ ] **Step 6: Commit**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
git add rust/Cargo.toml rust/.gitignore rust/src/main.rs
git commit -m "chore(rust): setup inicial do projeto cargo com deps axum askama rusqlite"
```

---

### Task 2: SQLite connection + ensure schema

**Files:**
- Create: `rust/src/db.rs`
- Modify: `rust/src/main.rs`

- [ ] **Step 1: criar `rust/src/db.rs`**

Crie `rust/src/db.rs`:
```rust
use rusqlite::Connection;
use std::path::Path;
use std::sync::{Arc, Mutex};

pub type Db = Arc<Mutex<Connection>>;

pub fn open_and_ensure_schema(db_path: &str, schema_path: &str) -> rusqlite::Result<Db> {
    // garante que o diretorio shared/ exista (em caso de primeira execucao)
    if let Some(parent) = Path::new(db_path).parent() {
        if !parent.as_os_str().is_empty() {
            std::fs::create_dir_all(parent).ok();
        }
    }

    let conn = Connection::open(db_path)?;

    // WAL e foreign keys
    conn.pragma_update(None, "journal_mode", "WAL")?;
    conn.pragma_update(None, "foreign_keys", "ON")?;

    // le schema.sql e executa idempotentemente
    let schema = std::fs::read_to_string(schema_path)
        .unwrap_or_else(|e| panic!("falha lendo schema {schema_path}: {e}"));
    conn.execute_batch(&schema)?;

    Ok(Arc::new(Mutex::new(conn)))
}
```

- [ ] **Step 2: wirear `db` em `main.rs` (smoke)**

Substitua `rust/src/main.rs` por:
```rust
mod db;

fn main() {
    // db relativo ao cwd quando rodar com `cargo run` dentro de rust/
    let conn = db::open_and_ensure_schema("../shared/expenses.db", "../shared/schema.sql")
        .expect("falha abrindo banco");
    let c = conn.lock().unwrap();
    let count: i64 = c
        .query_row("SELECT COUNT(*) FROM expenses", [], |r| r.get(0))
        .unwrap();
    println!("expenses: {count}");
}
```

- [ ] **Step 3: rodar pra verificar**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
cd rust
cargo run
cd ..
```

Esperado: `expenses: 0` (ou N existente).

- [ ] **Step 4: Commit**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
git add rust/src/db.rs rust/src/main.rs
git commit -m "feat(rust): conexao sqlite com ensure schema idempotente"
```

---

### Task 3: Domain type Expense

**Files:**
- Create: `rust/src/expense.rs`
- Modify: `rust/src/main.rs`

- [ ] **Step 1: criar `rust/src/expense.rs` so com struct**

Crie `rust/src/expense.rs`:
```rust
#[derive(Debug, Clone)]
pub struct Expense {
    pub id: i64,
    pub nome: String,
    pub valor: f64,
    pub descricao: String,
    pub total_acumulado: f64,
}
```

- [ ] **Step 2: registrar modulo em `main.rs`**

Substitua `rust/src/main.rs` por:
```rust
mod db;
mod expense;

fn main() {
    let conn = db::open_and_ensure_schema("../shared/expenses.db", "../shared/schema.sql")
        .expect("falha abrindo banco");
    let c = conn.lock().unwrap();
    let count: i64 = c
        .query_row("SELECT COUNT(*) FROM expenses", [], |r| r.get(0))
        .unwrap();
    println!("expenses: {count}");
    let _ = expense::Expense {
        id: 0,
        nome: String::new(),
        valor: 0.0,
        descricao: String::new(),
        total_acumulado: 0.0,
    };
}
```

- [ ] **Step 3: verificar build**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
cd rust
cargo build
cd ..
```

- [ ] **Step 4: Commit**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
git add rust/src/expense.rs rust/src/main.rs
git commit -m "feat(rust): struct expense do dominio"
```

---

### Task 4: Data access layer (repository)

**Files:**
- Modify: `rust/src/expense.rs`

- [ ] **Step 1: adicionar funcoes de repositorio**

Substitua `rust/src/expense.rs` por:
```rust
use crate::db::Db;
use rusqlite::params;

#[derive(Debug, Clone)]
pub struct Expense {
    pub id: i64,
    pub nome: String,
    pub valor: f64,
    pub descricao: String,
    pub total_acumulado: f64,
}

pub const PAGE_SIZE: i64 = 20;

/// retorna (itens da pagina, total_count)
pub fn list_page(db: &Db, page: i64) -> rusqlite::Result<(Vec<Expense>, i64)> {
    let page = if page < 1 { 1 } else { page };
    let offset = (page - 1) * PAGE_SIZE;

    let conn = db.lock().unwrap();

    let total: i64 = conn.query_row("SELECT COUNT(*) FROM expenses", [], |r| r.get(0))?;

    let mut stmt = conn.prepare(
        "SELECT id, nome, valor, descricao, \
                SUM(valor) OVER (ORDER BY id) AS total_acumulado \
           FROM expenses \
          ORDER BY id \
          LIMIT ?1 OFFSET ?2",
    )?;

    let rows = stmt
        .query_map(params![PAGE_SIZE, offset], |row| {
            Ok(Expense {
                id: row.get(0)?,
                nome: row.get(1)?,
                valor: row.get(2)?,
                descricao: row.get(3)?,
                total_acumulado: row.get(4)?,
            })
        })?
        .collect::<Result<Vec<_>, _>>()?;

    Ok((rows, total))
}

pub fn get_by_id(db: &Db, id: i64) -> rusqlite::Result<Option<Expense>> {
    let conn = db.lock().unwrap();
    let res = conn.query_row(
        "SELECT id, nome, valor, descricao, 0.0 as total_acumulado \
           FROM expenses WHERE id = ?1",
        params![id],
        |row| {
            Ok(Expense {
                id: row.get(0)?,
                nome: row.get(1)?,
                valor: row.get(2)?,
                descricao: row.get(3)?,
                total_acumulado: row.get(4)?,
            })
        },
    );
    match res {
        Ok(e) => Ok(Some(e)),
        Err(rusqlite::Error::QueryReturnedNoRows) => Ok(None),
        Err(e) => Err(e),
    }
}

pub fn insert(db: &Db, nome: &str, valor: f64, descricao: &str) -> rusqlite::Result<i64> {
    let conn = db.lock().unwrap();
    conn.execute(
        "INSERT INTO expenses (nome, valor, descricao) VALUES (?1, ?2, ?3)",
        params![nome, valor, descricao],
    )?;
    Ok(conn.last_insert_rowid())
}

pub fn update(db: &Db, id: i64, nome: &str, valor: f64, descricao: &str) -> rusqlite::Result<bool> {
    let conn = db.lock().unwrap();
    let n = conn.execute(
        "UPDATE expenses SET nome = ?1, valor = ?2, descricao = ?3 WHERE id = ?4",
        params![nome, valor, descricao, id],
    )?;
    Ok(n > 0)
}

pub fn zerar(db: &Db, id: i64) -> rusqlite::Result<bool> {
    let conn = db.lock().unwrap();
    let n = conn.execute("UPDATE expenses SET valor = 0 WHERE id = ?1", params![id])?;
    Ok(n > 0)
}

pub fn sum_all(db: &Db) -> rusqlite::Result<f64> {
    let conn = db.lock().unwrap();
    conn.query_row("SELECT COALESCE(SUM(valor), 0) FROM expenses", [], |r| {
        r.get::<_, f64>(0)
    })
}

pub fn sum_excluding(db: &Db, excluding_id: i64) -> rusqlite::Result<f64> {
    let conn = db.lock().unwrap();
    conn.query_row(
        "SELECT COALESCE(SUM(valor), 0) FROM expenses WHERE id != ?1",
        params![excluding_id],
        |r| r.get::<_, f64>(0),
    )
}

pub fn total_pages(total_count: i64) -> i64 {
    if total_count <= 0 {
        1
    } else {
        (total_count + PAGE_SIZE - 1) / PAGE_SIZE
    }
}
```

- [ ] **Step 2: smoke test em `main.rs`**

Substitua `rust/src/main.rs` por:
```rust
mod db;
mod expense;

fn main() {
    let conn = db::open_and_ensure_schema("../shared/expenses.db", "../shared/schema.sql")
        .expect("falha abrindo banco");
    let (rows, total) = expense::list_page(&conn, 1).unwrap();
    println!("total = {total}, primeira pagina = {} itens", rows.len());
}
```

- [ ] **Step 3: build**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
cd rust
cargo build
cd ..
```

- [ ] **Step 4: Commit**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
git add rust/src/expense.rs rust/src/main.rs
git commit -m "feat(rust): repositorio de expenses com paginacao e running sum"
```

---

### Task 5: Expression parser

**Files:**
- Create: `rust/src/parser.rs`
- Modify: `rust/src/main.rs`

- [ ] **Step 1: criar `rust/src/parser.rs`**

Crie `rust/src/parser.rs`:
```rust
use evalexpr::{eval, Value};

#[derive(Debug)]
pub enum ParseError {
    Empty,
    InvalidNumber,
    InvalidExpression(String),
    DivisionByZero,
    NotFinite,
}

impl std::fmt::Display for ParseError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            ParseError::Empty => write!(f, "valor obrigatorio"),
            ParseError::InvalidNumber => write!(f, "nao e um numero valido"),
            ParseError::InvalidExpression(m) => write!(f, "expressao invalida: {m}"),
            ParseError::DivisionByZero => write!(f, "divisao por zero"),
            ParseError::NotFinite => write!(f, "resultado nao finito"),
        }
    }
}

/// regras do spec:
/// - se input comeca com '=' -> avalia como expressao (so +,-,*,/,(,) e numeros com '.')
/// - senao -> trata como numero literal, aceita ',' ou '.' como decimal
pub fn parse_valor(input: &str) -> Result<f64, ParseError> {
    let s = input.trim();
    if s.is_empty() {
        return Err(ParseError::Empty);
    }

    if let Some(expr) = s.strip_prefix('=') {
        let expr = expr.trim();
        if expr.is_empty() {
            return Err(ParseError::Empty);
        }
        // virgula e reservada em modo expressao; rejeita
        if expr.contains(',') {
            return Err(ParseError::InvalidExpression(
                "use ponto como decimal em expressoes".to_string(),
            ));
        }
        // sanity check de caracteres permitidos
        for c in expr.chars() {
            let ok = c.is_ascii_digit()
                || matches!(c, '+' | '-' | '*' | '/' | '(' | ')' | '.' | ' ' | '\t');
            if !ok {
                return Err(ParseError::InvalidExpression(format!(
                    "caractere nao permitido: '{c}'"
                )));
            }
        }
        match eval(expr) {
            Ok(Value::Float(n)) => check_finite(n),
            Ok(Value::Int(n)) => check_finite(n as f64),
            Ok(other) => Err(ParseError::InvalidExpression(format!(
                "tipo nao numerico: {other:?}"
            ))),
            Err(e) => {
                let msg = e.to_string();
                if msg.to_lowercase().contains("division") {
                    Err(ParseError::DivisionByZero)
                } else {
                    Err(ParseError::InvalidExpression(msg))
                }
            }
        }
    } else {
        // numero literal: aceita ',' ou '.'
        let normalized = s.replace(',', ".");
        match normalized.parse::<f64>() {
            Ok(n) => check_finite(n),
            Err(_) => Err(ParseError::InvalidNumber),
        }
    }
}

fn check_finite(n: f64) -> Result<f64, ParseError> {
    if n.is_finite() {
        Ok(n)
    } else {
        Err(ParseError::NotFinite)
    }
}
```

- [ ] **Step 2: registrar modulo + smoke**

Substitua `rust/src/main.rs` por:
```rust
mod db;
mod expense;
mod parser;

fn main() {
    let cases = ["100", "100,50", "-50", "=50+10", "=(-1*(50/2)+10)", "=10/0", "abc"];
    for c in cases {
        println!("{c} -> {:?}", parser::parse_valor(c));
    }
}
```

- [ ] **Step 3: rodar pra validar**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
cd rust
cargo run
cd ..
```

Esperado: 100, 100.5, -50, 60, -15, erro divisao por zero, erro numero invalido.

- [ ] **Step 4: Commit**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
git add rust/src/parser.rs rust/src/main.rs
git commit -m "feat(rust): parser de valor com evalexpr sandboxed"
```

---

### Task 6: Currency formatting BRL pt-BR

**Files:**
- Create: `rust/src/format.rs`
- Modify: `rust/src/main.rs`

- [ ] **Step 1: criar `rust/src/format.rs`**

Crie `rust/src/format.rs`:
```rust
/// formata um f64 como BRL pt-BR: "R$ 1.234,56" ou "-R$ 50,00"
pub fn format_brl(n: f64) -> String {
    let negative = n < 0.0;
    let abs = n.abs();
    // arredonda pra 2 casas
    let cents = (abs * 100.0).round() as i64;
    let int_part = cents / 100;
    let frac_part = cents % 100;

    // milhar com ponto
    let int_str = int_part.to_string();
    let mut with_dots = String::new();
    let chars: Vec<char> = int_str.chars().collect();
    for (i, c) in chars.iter().enumerate() {
        if i > 0 && (chars.len() - i) % 3 == 0 {
            with_dots.push('.');
        }
        with_dots.push(*c);
    }

    let body = format!("R$ {with_dots},{frac_part:02}");
    if negative {
        format!("-{body}")
    } else {
        body
    }
}

pub fn is_negative(n: f64) -> bool {
    n < 0.0
}
```

- [ ] **Step 2: smoke em `main.rs`**

Substitua `rust/src/main.rs` por:
```rust
mod db;
mod expense;
mod format;
mod parser;

fn main() {
    for v in [0.0_f64, 100.0, 1234.56, -50.0, 1_234_567.89, -0.5] {
        println!("{v} -> {}", format::format_brl(v));
    }
}
```

- [ ] **Step 3: rodar e conferir**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
cd rust
cargo run
cd ..
```

Esperado:
```
0 -> R$ 0,00
100 -> R$ 100,00
1234.56 -> R$ 1.234,56
-50 -> -R$ 50,00
1234567.89 -> R$ 1.234.567,89
-0.5 -> -R$ 0,50
```

- [ ] **Step 4: Commit**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
git add rust/src/format.rs rust/src/main.rs
git commit -m "feat(rust): formatacao brl pt-br"
```

---

### Task 7: Layout template (Askama)

**Files:**
- Create: `rust/templates/layout.html`

- [ ] **Step 1: criar diretorio + arquivo**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
New-Item -ItemType Directory -Force "rust\templates" | Out-Null
```

- [ ] **Step 2: criar `rust/templates/layout.html`**

Crie `rust/templates/layout.html`:
```html
<!doctype html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>estudoHtmx - rust</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <script src="https://unpkg.com/htmx.org@1.9.12"></script>
  <style>
    .neg { color: var(--pico-color-red-500, #c0392b); }
    .actions button { padding: 0.25rem 0.5rem; }
    .truncate { max-width: 18ch; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; display: inline-block; vertical-align: bottom; }
    small.error { color: var(--pico-color-red-500, #c0392b); display: block; }
    small.hint { color: var(--pico-muted-color, #6c757d); display: block; }
    .pagination { display: flex; gap: .5rem; align-items: center; justify-content: center; margin-top: 1rem; }
  </style>
</head>
<body>
  <main class="container">
    <h1>despesas (rust)</h1>
    <section id="form-section">
      {{ form_html|safe }}
    </section>
    <section id="list-section">
      {{ list_html|safe }}
    </section>
  </main>
</body>
</html>
```

- [ ] **Step 3: Commit**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
git add rust/templates/layout.html
git commit -m "feat(rust): layout askama com pico e bootstrap icons via cdn"
```

---

### Task 8: Form template

**Files:**
- Create: `rust/templates/form.html`

- [ ] **Step 1: criar `rust/templates/form.html`**

Crie `rust/templates/form.html`:
```html
<form id="expense-form"
      hx-post="/expenses"
      hx-target="#expense-form"
      hx-swap="outerHTML"
      {% if oob %}hx-swap-oob="true"{% endif %}>
  <input type="hidden" name="id" value="{% if let Some(id) = id %}{{ id }}{% endif %}">

  <label for="nome">nome
    <input id="nome" name="nome" type="text" value="{{ nome }}"
           aria-invalid="{% if error_nome.is_some() %}true{% else %}false{% endif %}">
    {% if let Some(e) = error_nome %}<small class="error">{{ e }}</small>{% endif %}
  </label>

  <label for="valor">valor
    <input id="valor" name="valor" type="text" value="{{ valor }}"
           aria-invalid="{% if error_valor.is_some() %}true{% else %}false{% endif %}">
    <small class="hint">ex: 100, -50, 1234,56 ou =(50/2)+10</small>
    {% if let Some(e) = error_valor %}<small class="error">{{ e }}</small>{% endif %}
  </label>

  <label for="total_acumulado">total acumulado
    <input id="total_acumulado" name="total_acumulado" type="text" readonly
           value="{{ total_acumulado_fmt }}"
           hx-get="/expenses/total-context?excluding_id={% if let Some(id) = id %}{{ id }}{% endif %}"
           hx-trigger="focusout from:#expense-form input delay:300ms, focusout from:#expense-form textarea delay:300ms"
           hx-target="this"
           hx-swap="outerHTML">
  </label>

  <label for="descricao">descricao
    <textarea id="descricao" name="descricao" rows="2">{{ descricao }}</textarea>
  </label>

  <div role="group">
    <button type="submit">salvar</button>
    <button type="button" class="secondary"
            hx-get="/expenses/form"
            hx-target="#expense-form"
            hx-swap="outerHTML">cancelar</button>
  </div>
</form>
```

- [ ] **Step 2: criar `rust/templates/total_context.html`**

Crie `rust/templates/total_context.html`:
```html
<input id="total_acumulado" name="total_acumulado" type="text" readonly
       value="{{ total_fmt }}"
       hx-get="/expenses/total-context?excluding_id={% if let Some(id) = excluding_id %}{{ id }}{% endif %}"
       hx-trigger="focusout from:#expense-form input delay:300ms, focusout from:#expense-form textarea delay:300ms"
       hx-target="this"
       hx-swap="outerHTML">
```

- [ ] **Step 3: Commit**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
git add rust/templates/form.html rust/templates/total_context.html
git commit -m "feat(rust): templates do form e do total-context"
```

---

### Task 9: List template

**Files:**
- Create: `rust/templates/list.html`

- [ ] **Step 1: criar `rust/templates/list.html`**

Crie `rust/templates/list.html`:
```html
<table id="expense-table">
  <thead>
    <tr>
      <th>#</th>
      <th>nome</th>
      <th>valor</th>
      <th>total acumulado</th>
      <th>descricao</th>
      <th>acoes</th>
    </tr>
  </thead>
  <tbody id="expense-list" {% if oob %}hx-swap-oob="true"{% endif %}>
    {% if items.is_empty() %}
      <tr><td colspan="6"><em>nenhuma despesa cadastrada</em></td></tr>
    {% else %}
      {% for it in items %}
      <tr>
        <td>{{ it.id }}</td>
        <td>{{ it.nome }}</td>
        <td class="{% if it.valor < 0.0 %}neg{% endif %}">{{ crate::format::format_brl(it.valor) }}</td>
        <td class="{% if it.total_acumulado < 0.0 %}neg{% endif %}">{{ crate::format::format_brl(it.total_acumulado) }}</td>
        <td><span class="truncate" title="{{ it.descricao }}">{{ it.descricao }}</span></td>
        <td class="actions">
          <button type="button"
                  hx-get="/expenses/form?id={{ it.id }}"
                  hx-target="#expense-form"
                  hx-swap="outerHTML"
                  aria-label="editar">
            <i class="bi bi-pencil"></i>
          </button>
          <button type="button" class="secondary"
                  hx-post="/expenses/{{ it.id }}/zerar"
                  hx-confirm="Zerar este item?"
                  hx-target="#expense-form"
                  hx-swap="outerHTML"
                  aria-label="zerar">
            <i class="bi bi-arrow-counterclockwise"></i>
          </button>
        </td>
      </tr>
      {% endfor %}
    {% endif %}
  </tbody>
</table>

<nav class="pagination" aria-label="paginacao">
  <button type="button"
          {% if page <= 1 %}disabled{% endif %}
          hx-get="/expenses?page=1"
          hx-target="#list-section"
          hx-swap="innerHTML"
          aria-label="primeira">&lt;&lt;</button>
  <button type="button"
          {% if page <= 1 %}disabled{% endif %}
          hx-get="/expenses?page={{ page - 1 }}"
          hx-target="#list-section"
          hx-swap="innerHTML"
          aria-label="anterior">&lt;</button>
  <span>pagina {{ page }} de {{ total_pages }}</span>
  <button type="button"
          {% if page >= total_pages %}disabled{% endif %}
          hx-get="/expenses?page={{ page + 1 }}"
          hx-target="#list-section"
          hx-swap="innerHTML"
          aria-label="proxima">&gt;</button>
  <button type="button"
          {% if page >= total_pages %}disabled{% endif %}
          hx-get="/expenses?page={{ total_pages }}"
          hx-target="#list-section"
          hx-swap="innerHTML"
          aria-label="ultima">&gt;&gt;</button>
</nav>
```

- [ ] **Step 2: Commit**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
git add rust/templates/list.html
git commit -m "feat(rust): template da lista com paginacao e acoes"
```

---

### Task 10: Route GET /

**Files:**
- Create: `rust/src/handlers.rs`
- Modify: `rust/src/main.rs`

- [ ] **Step 1: criar `rust/src/handlers.rs` com templates Askama e GET /**

Crie `rust/src/handlers.rs`:
```rust
use crate::db::Db;
use crate::expense::{self, Expense};
use crate::format::format_brl;
use crate::parser;
use askama::Template;
use askama_axum::IntoResponse;
use axum::extract::{Form, Path, Query, State};
use axum::http::{HeaderMap, StatusCode};
use axum::response::Html;
use serde::Deserialize;

#[derive(Clone)]
pub struct AppState {
    pub db: Db,
}

// ---------- templates ----------

#[derive(Template)]
#[template(path = "form.html")]
pub struct FormTpl {
    pub id: Option<i64>,
    pub nome: String,
    pub valor: String,
    pub descricao: String,
    pub total_acumulado_fmt: String,
    pub error_nome: Option<String>,
    pub error_valor: Option<String>,
    pub oob: bool,
}

impl FormTpl {
    pub fn empty(total: f64) -> Self {
        Self {
            id: None,
            nome: String::new(),
            valor: String::new(),
            descricao: String::new(),
            total_acumulado_fmt: format_brl(total),
            error_nome: None,
            error_valor: None,
            oob: false,
        }
    }
}

#[derive(Template)]
#[template(path = "list.html")]
pub struct ListTpl {
    pub items: Vec<Expense>,
    pub page: i64,
    pub total_pages: i64,
    pub oob: bool,
}

#[derive(Template)]
#[template(path = "layout.html", escape = "none")]
pub struct LayoutTpl {
    pub form_html: String,
    pub list_html: String,
}

#[derive(Template)]
#[template(path = "total_context.html")]
pub struct TotalContextTpl {
    pub total_fmt: String,
    pub excluding_id: Option<i64>,
}

// helper combinado pra POST /expenses (form + tbody oob)
pub struct CombinedFragment {
    pub form_html: String,
    pub list_html: String,
}

impl IntoResponse for CombinedFragment {
    fn into_response(self) -> axum::response::Response {
        Html(format!("{}\n{}", self.form_html, self.list_html)).into_response()
    }
}

// ---------- handlers ----------

pub async fn index(State(s): State<AppState>) -> impl IntoResponse {
    let (items, total_count) = expense::list_page(&s.db, 1).unwrap_or((vec![], 0));
    let total_sum = expense::sum_all(&s.db).unwrap_or(0.0);
    let total_pages = expense::total_pages(total_count);

    let form = FormTpl::empty(total_sum);
    let list = ListTpl {
        items,
        page: 1,
        total_pages,
        oob: false,
    };

    let layout = LayoutTpl {
        form_html: form.render().unwrap_or_default(),
        list_html: list.render().unwrap_or_default(),
    };
    Html(layout.render().unwrap_or_default())
}

// stubs preenchidos nas proximas tasks
#[derive(Deserialize)]
pub struct PageQuery {
    pub page: Option<i64>,
}

pub async fn get_expenses(
    State(_s): State<AppState>,
    Query(_q): Query<PageQuery>,
) -> impl IntoResponse {
    (StatusCode::OK, Html("<p>todo</p>"))
}

#[derive(Deserialize)]
pub struct FormQuery {
    pub id: Option<i64>,
}

pub async fn get_form(
    State(_s): State<AppState>,
    Query(_q): Query<FormQuery>,
) -> impl IntoResponse {
    (StatusCode::OK, Html("<p>todo</p>"))
}

#[derive(Deserialize)]
pub struct TotalContextQuery {
    pub excluding_id: Option<i64>,
}

pub async fn get_total_context(
    State(_s): State<AppState>,
    Query(_q): Query<TotalContextQuery>,
) -> impl IntoResponse {
    (StatusCode::OK, Html("<p>todo</p>"))
}

#[derive(Deserialize)]
pub struct ExpenseForm {
    pub id: Option<String>,
    pub nome: Option<String>,
    pub valor: Option<String>,
    pub descricao: Option<String>,
}

pub async fn post_expense(
    State(_s): State<AppState>,
    _headers: HeaderMap,
    Form(_f): Form<ExpenseForm>,
) -> impl IntoResponse {
    (StatusCode::OK, Html("<p>todo</p>"))
}

pub async fn post_zerar(
    State(_s): State<AppState>,
    Path(_id): Path<i64>,
) -> impl IntoResponse {
    (StatusCode::OK, Html("<p>todo</p>"))
}

// permite usar funcoes do crate dentro dos templates Askama
pub mod template_helpers {
    pub use crate::format::format_brl;
    pub use crate::parser;
    let _ = parser::parse_valor;
}
```

Nota: remova o bloco `pub mod template_helpers` se gerar warning; ele esta so como reminder.

Substitua o final do arquivo (removendo o bloco problematico):
```rust
// fim do arquivo
```

Versao limpa do final: apague o bloco `pub mod template_helpers { ... }` se voce o copiou. Mantenha apenas ate `pub async fn post_zerar`.

- [ ] **Step 2: registrar router em `main.rs`**

Substitua `rust/src/main.rs` por:
```rust
mod db;
mod expense;
mod format;
mod handlers;
mod parser;

use axum::routing::{get, post};
use axum::Router;
use handlers::AppState;
use std::net::SocketAddr;

#[tokio::main]
async fn main() {
    let db = db::open_and_ensure_schema("../shared/expenses.db", "../shared/schema.sql")
        .expect("falha abrindo banco");
    let state = AppState { db };

    let app = Router::new()
        .route("/", get(handlers::index))
        .route("/expenses", get(handlers::get_expenses).post(handlers::post_expense))
        .route("/expenses/form", get(handlers::get_form))
        .route("/expenses/total-context", get(handlers::get_total_context))
        .route("/expenses/:id/zerar", post(handlers::post_zerar))
        .with_state(state);

    let addr: SocketAddr = "127.0.0.1:5005".parse().unwrap();
    println!("estudoHtmx-rust ouvindo em http://{addr}");
    let listener = tokio::net::TcpListener::bind(addr).await.unwrap();
    axum::serve(listener, app).await.unwrap();
}
```

- [ ] **Step 3: build + rodar pra smoke test**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
cd rust
cargo build
cd ..
```

Se compilar, sucesso. (Nao precisa subir o servidor ainda.)

- [ ] **Step 4: Commit**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
git add rust/src/handlers.rs rust/src/main.rs
git commit -m "feat(rust): router axum e handler get /"
```

---

### Task 11: Route GET /expenses?page=N

**Files:**
- Modify: `rust/src/handlers.rs`

- [ ] **Step 1: substituir `get_expenses` por versao completa**

Em `rust/src/handlers.rs`, substitua a funcao `get_expenses` (a stub) por:
```rust
pub async fn get_expenses(
    State(s): State<AppState>,
    Query(q): Query<PageQuery>,
) -> impl IntoResponse {
    let page = q.page.unwrap_or(1).max(1);
    let (items, total_count) = expense::list_page(&s.db, page).unwrap_or((vec![], 0));
    let total_pages = expense::total_pages(total_count);
    let effective_page = if page > total_pages { total_pages } else { page };

    let (items, _total) = if effective_page != page {
        expense::list_page(&s.db, effective_page).unwrap_or((items, total_count))
    } else {
        (items, total_count)
    };

    let list = ListTpl {
        items,
        page: effective_page,
        total_pages,
        oob: false,
    };
    Html(list.render().unwrap_or_default())
}
```

- [ ] **Step 2: build**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
cd rust
cargo build
cd ..
```

- [ ] **Step 3: Commit**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
git add rust/src/handlers.rs
git commit -m "feat(rust): get /expenses com paginacao"
```

---

### Task 12: Route GET /expenses/form?id=N

**Files:**
- Modify: `rust/src/handlers.rs`

- [ ] **Step 1: substituir `get_form`**

Em `rust/src/handlers.rs`, substitua a funcao `get_form` por:
```rust
pub async fn get_form(
    State(s): State<AppState>,
    Query(q): Query<FormQuery>,
) -> impl IntoResponse {
    let total = match q.id {
        Some(id) => expense::sum_excluding(&s.db, id).unwrap_or(0.0),
        None => expense::sum_all(&s.db).unwrap_or(0.0),
    };

    let form = if let Some(id) = q.id {
        match expense::get_by_id(&s.db, id) {
            Ok(Some(e)) => FormTpl {
                id: Some(e.id),
                nome: e.nome,
                valor: format!("{}", e.valor),
                descricao: e.descricao,
                total_acumulado_fmt: format_brl(total),
                error_nome: None,
                error_valor: None,
                oob: false,
            },
            _ => FormTpl::empty(total),
        }
    } else {
        FormTpl::empty(total)
    };

    Html(form.render().unwrap_or_default())
}
```

- [ ] **Step 2: build**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
cd rust
cargo build
cd ..
```

- [ ] **Step 3: Commit**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
git add rust/src/handlers.rs
git commit -m "feat(rust): get /expenses/form com edicao por id"
```

---

### Task 13: Route GET /expenses/total-context

**Files:**
- Modify: `rust/src/handlers.rs`

- [ ] **Step 1: substituir `get_total_context`**

Em `rust/src/handlers.rs`, substitua a funcao `get_total_context` por:
```rust
pub async fn get_total_context(
    State(s): State<AppState>,
    Query(q): Query<TotalContextQuery>,
) -> impl IntoResponse {
    let total = match q.excluding_id {
        Some(id) => expense::sum_excluding(&s.db, id).unwrap_or(0.0),
        None => expense::sum_all(&s.db).unwrap_or(0.0),
    };
    let tpl = TotalContextTpl {
        total_fmt: format_brl(total),
        excluding_id: q.excluding_id,
    };
    Html(tpl.render().unwrap_or_default())
}
```

- [ ] **Step 2: build**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
cd rust
cargo build
cd ..
```

- [ ] **Step 3: Commit**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
git add rust/src/handlers.rs
git commit -m "feat(rust): get /expenses/total-context"
```

---

### Task 14: Route POST /expenses

**Files:**
- Modify: `rust/src/handlers.rs`

- [ ] **Step 1: substituir `post_expense`**

Em `rust/src/handlers.rs`, substitua a funcao `post_expense` por:
```rust
pub async fn post_expense(
    State(s): State<AppState>,
    _headers: HeaderMap,
    Form(f): Form<ExpenseForm>,
) -> impl IntoResponse {
    let id_opt: Option<i64> = f
        .id
        .as_deref()
        .and_then(|s| s.trim().parse::<i64>().ok())
        .filter(|n| *n > 0);

    let nome_raw = f.nome.unwrap_or_default();
    let valor_raw = f.valor.unwrap_or_default();
    let descricao = f.descricao.unwrap_or_default();

    let nome = nome_raw.trim().to_string();
    let mut error_nome = None;
    let mut error_valor = None;

    if nome.is_empty() {
        error_nome = Some("nome obrigatorio".to_string());
    }

    let parsed_valor = match parser::parse_valor(&valor_raw) {
        Ok(v) => Some(v),
        Err(_e) => {
            // mensagem padronizada cross-stack (ver spec sec.8). variantes
            // granulares de ParseError ficam pra logs internos.
            error_valor = Some("valor invalido".to_string());
            None
        }
    };

    if error_nome.is_some() || error_valor.is_some() {
        // re-renderiza form com erros, sem mexer no banco. nao manda tbody oob.
        let total = match id_opt {
            Some(id) => expense::sum_excluding(&s.db, id).unwrap_or(0.0),
            None => expense::sum_all(&s.db).unwrap_or(0.0),
        };
        let form = FormTpl {
            id: id_opt,
            nome: nome_raw,
            valor: valor_raw,
            descricao,
            total_acumulado_fmt: format_brl(total),
            error_nome,
            error_valor,
            oob: false,
        };
        return Html(form.render().unwrap_or_default()).into_response();
    }

    let valor = parsed_valor.unwrap();

    // valido: insere ou atualiza
    match id_opt {
        Some(id) => {
            let _ = expense::update(&s.db, id, &nome, valor, &descricao);
        }
        None => {
            let _ = expense::insert(&s.db, &nome, valor, &descricao);
        }
    }

    // re-renderiza form zerado + tbody oob da pagina 1
    let total_sum = expense::sum_all(&s.db).unwrap_or(0.0);
    let (items, total_count) = expense::list_page(&s.db, 1).unwrap_or((vec![], 0));
    let total_pages = expense::total_pages(total_count);

    let mut form = FormTpl::empty(total_sum);
    form.oob = true; // hx-swap-oob="true" no proprio form

    let list = ListTpl {
        items,
        page: 1,
        total_pages,
        oob: true,
    };

    CombinedFragment {
        form_html: form.render().unwrap_or_default(),
        list_html: list.render().unwrap_or_default(),
    }
    .into_response()
}
```

Nota: nesse modelo o front faz `hx-target="#expense-form"` e o form retornado tem `hx-swap-oob="true"` no proprio elemento. Para garantir comportamento consistente, o handler retorna ambos (form + tbody) e o HTMX swap-oob processa o tbody. O target principal do swap recebe o form (que tambem tem oob="true", mas funciona pois o hx-target casa com o id).

- [ ] **Step 2: build**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
cd rust
cargo build
cd ..
```

- [ ] **Step 3: Commit**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
git add rust/src/handlers.rs
git commit -m "feat(rust): post /expenses com validacao e oob"
```

---

### Task 15: Route POST /expenses/{id}/zerar

**Files:**
- Modify: `rust/src/handlers.rs`

- [ ] **Step 1: substituir `post_zerar`**

Em `rust/src/handlers.rs`, substitua a funcao `post_zerar` por:
```rust
pub async fn post_zerar(
    State(s): State<AppState>,
    Path(id): Path<i64>,
) -> impl IntoResponse {
    let _ = expense::zerar(&s.db, id);

    let total_sum = expense::sum_all(&s.db).unwrap_or(0.0);
    let (items, total_count) = expense::list_page(&s.db, 1).unwrap_or((vec![], 0));
    let total_pages = expense::total_pages(total_count);

    let mut form = FormTpl::empty(total_sum);
    form.oob = true;

    let list = ListTpl {
        items,
        page: 1,
        total_pages,
        oob: true,
    };

    CombinedFragment {
        form_html: form.render().unwrap_or_default(),
        list_html: list.render().unwrap_or_default(),
    }
    .into_response()
}
```

- [ ] **Step 2: build + smoke run completo**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
cd rust
cargo build
cd ..
```

Pra testar manualmente:
```powershell
cd rust
cargo run
```
Abrir http://localhost:5005, criar, editar, zerar, paginar. `Ctrl+C` pra parar.

- [ ] **Step 3: Commit**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
git add rust/src/handlers.rs
git commit -m "feat(rust): post /expenses/:id/zerar"
```

---

### Task 16: README

**Files:**
- Create: `rust/README.md`

- [ ] **Step 1: criar `rust/README.md`**

Crie `rust/README.md`:
```markdown
# estudoHtmx — rust (axum + askama)

backend rust do estudo. compartilha `../shared/expenses.db` com os outros 4 stacks.

## requisitos

- rust 1.75+ (edition 2021)
- cargo

## install

a partir da raiz do repositorio:

```powershell
cd rust
cargo build
```

## run

```powershell
cd rust
cargo run
```

ou release:

```powershell
cargo run --release
```

opcional, hot reload:

```powershell
cargo install cargo-watch
cargo watch -x run
```

a aplicacao sobe em **http://localhost:5005**.

## estrutura

```
rust/
├── Cargo.toml
├── src/
│   ├── main.rs        # entrypoint + router
│   ├── db.rs          # connection + ensure schema
│   ├── expense.rs     # struct + repository
│   ├── parser.rs      # evalexpr wrapper
│   ├── format.rs      # BRL pt-BR
│   └── handlers.rs    # axum handlers + askama templates
└── templates/
    ├── layout.html
    ├── form.html
    ├── list.html
    └── total_context.html
```

## banco

ao subir, le `../shared/schema.sql` e aplica idempotentemente em `../shared/expenses.db`. WAL e foreign keys ligados.
```

- [ ] **Step 2: Commit**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
git add rust/README.md
git commit -m "docs(rust): readme com install e run"
```

---

### Task 17: Acceptance test checklist

**Files:**
- (nenhum arquivo novo — executar manualmente)

- [ ] **Step 1: subir o servidor**

Run from `C:\Dev\github\estudoHtmx`:
```powershell
cd rust
cargo run
```

- [ ] **Step 2: rodar o checklist do spec**

Abra http://localhost:5005 no navegador e verifique:

- [ ] subir o servidor, abrir no browser, ver pagina completa
- [ ] criar uma despesa com valor literal (ex: `100`)
- [ ] criar uma despesa com expressao (ex: `=(-1*(50/2)+10)`)
- [ ] editar a despesa criada (clicar editar, alterar, salvar)
- [ ] validar erro: salvar com nome vazio mostra erro inline
- [ ] validar erro: salvar com `valor=abc` mostra erro inline
- [ ] zerar uma despesa (item continua na lista com valor 0, soma 0)
- [ ] criar 25 despesas, navegar pra pagina 2, running sum continua corretamente
- [ ] total_acumulado do form se atualiza ao tabular pra fora do form
- [ ] dados persistem ao trocar de stack (mesmo banco)

- [ ] **Step 3: derrubar o servidor**

`Ctrl+C` no terminal.

- [ ] **Step 4: Commit final (se houver ajustes)**

Caso tenha feito qualquer correcao durante o checklist:
```powershell
git add rust
git commit -m "fix(rust): ajustes pos checklist de aceitacao"
```

Caso nada tenha mudado, pular o commit.
